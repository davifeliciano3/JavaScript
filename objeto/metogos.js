/**
 * Object.assing(des,any) faz uma cópia do objeto ...
 * Object.value(Retorna o valor)
 * Object.entries(Retorna chave e valor dentro de um array)
 * Object.keys(Retorna chave)
 * Object.freeze
 * Object.defineProperties(define varias propriedades)
 * Object.defineProperty(define uma propriedades)
 * Object.getOwnPropertyDescriptor(o,'prop) (retorna as propriedade de um objeto)
 * 
*/