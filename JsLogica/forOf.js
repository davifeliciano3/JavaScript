const frutas = ['banan','maca','pera'];
const nome = 'davi feliciano';

for (let valor of nome) {
    console.log(valor);
}

frutas.forEach(function (valor,indice) {
    console.log(valor);
})

for (let i in objeto) {//retorna o indice e chave pode ser usado com objeto
    console.log()
}